'use strict';

module.exports = {
  dependencies: {
    'react-viro': {
      platforms: {
        ios: null,
        android: null,
      },
    },
  },
  assets: ['./src/assets/fonts/'], // stays the same
};
